## Fixed
- 修复“复制给 AI 的 Prompt”在点击后无响应或静默失败的问题，补齐复制失败反馈与异常处理。
- 修复 AI Prompt 目标地址选择错误问题，避免错误使用本机会话地址导致提示词不可用。
- 修复按钮显示策略：仅在 client 会话且目标节点非 client 且存在虚拟 IP 时显示；server（serve）会话不显示。

## Changed
- 将“复制给 AI 的 Prompt”内容收敛为三行短模板，保留健康检查入口并改用 raw `SKILL.md` 链接。
- 统一复制类按钮交互反馈文案与可用性行为，减少误操作与状态不明。
