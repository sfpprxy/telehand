## Fixed
- 补齐 Linux 平台 EasyTier 嵌入二进制接入，修复 Linux 目标构建时缺少嵌入资产的问题。
- 修复发布流程仅产出 Windows/macOS 包的问题，新增 Linux `amd64` 与 `arm64` 构建与打包产物。

## Changed
- 新增 Linux 专用嵌入文件：`linux/amd64`、`linux/arm64` 分架构加载 EasyTier core/cli。
- 更新 `scripts/fetch-easytier.sh`、`build.sh` 与 README，统一 Linux 下载、构建与手动运行说明。
