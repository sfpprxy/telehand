准备实现&发布 0.4.0

# 实现规则

自底向上的进行实现, 完成一个子任务后就用 Markdown 语法标记完成。但是如果在任何过程中遇到了问题，则在子任务下新建一个子任务并描述问题，解决完问题也标记完成。一般来说现有的任务内容不需要改动，如需改动停下来征求我同意。

# 0.4.0 目标：正式支持 Linux（优先 amd64/arm64）并打通发布链路

## 方案与待实现（按依赖顺序）

- [x] Linux 平台二进制嵌入与运行时接入
  - [x] 新增 Linux 架构分离的 embed 文件（至少 `linux/amd64`、`linux/arm64`），分别注入 EasyTier core/cli。
  - [x] 保持 Windows/macOS 现有行为不回归，确保 `EasyTier.Start` 在 Linux 下正确使用嵌入产物启动。
  - [x] 明确 Linux 权限/能力要求（root 或 CAP_NET_ADMIN）并映射到现有错误码与提示。
- [x] EasyTier 资源拉取脚本补齐 Linux
  - [x] `scripts/fetch-easytier.sh` 增加 Linux 资产下载（amd64/arm64）与重命名落盘逻辑。
  - [x] 仅在缺失目标文件时下载，保持增量拉取语义与现有平台一致。
  - [x] 下载后统一 `chmod +x`，确保本地构建与 CI 一致可执行。
- [x] 构建与发布流程支持 Linux
  - [x] 更新本地 `build.sh`，新增 Linux 构建目标（至少 amd64，建议 arm64）。
  - [x] 更新 `.github/workflows/release.yml`，新增 Linux 构建矩阵、打包与上传。
  - [x] 产物命名保持 `telehand-linux-<arch>-vX.Y.Z.zip`，与 `install.sh` 约定一致。
- [x] 安装与文档对齐
  - [x] README 手动下载章节补充 Linux 产物说明与启动命令。
  - [x] 校验 `install.sh` 对 Linux 架构识别与发布产物一致，避免 arm64 下载失败。
  - [x] connect 输出给远端的 Linux 一键命令保持可用、无需额外人工步骤。
- [x] 测试与回归
  - [x] 运行 `go test ./...`，确保核心逻辑回归通过。
  - [x] 至少完成一次 Linux 目标编译验证（amd64；若支持 arm64 也验证）。
  - [x] 验证 Windows/macOS 目标仍可编译（至少 CI 矩阵层面可通过）。

## 验收标准

- [x] Release 流程生成 Linux 产物并可被 `install.sh` 成功安装。
- [ ] Linux 上 `telehand serve` / `telehand connect` 可启动并完成 EasyTier 初始化（权限满足前提下）。
- [x] 文档中的 Linux 安装与手动下载路径与实际发布文件一致。

## 发布状态

- [ ] 0.4.0 待在 Linux 实机完成 `serve/connect` 运行验收后进入发布。
