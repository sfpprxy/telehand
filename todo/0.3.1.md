准备实现 0.3.1

# 实现规则

自底向上的进行实现, 完成一个子任务后就用 Markdown 语法标记完成。但是如果在任何过程中遇到了问题，则在子任务下新建一个子任务并描述问题，解决完问题也标记完成。一般来说现有的任务内容不需要改动，如需改动停下来征求我同意。

# 0.3.1 目标：修复 Web UI “复制给 AI 的 Prompt”点击无效

## 待实现（按依赖顺序）

- [x] 前端复制链路修复（核心）
  - [x] 修正 `copyAIPromptForPeer` 的 endpoint 选择逻辑：优先使用目标 peer 的 `virtual_ipv4` + 当前 `api_port`，避免误用本机 `virt_ip`。
  - [x] 保留并明确兜底行为：当 peer 不可用时再回退到当前会话 endpoint，且需有可视化提示。
  - [x] 为复制失败补齐 `.catch(...)`，输出明确错误文案（如浏览器权限、非安全上下文、空 endpoint）。
- [x] 按钮可用性与交互反馈
  - [x] 在不满足复制条件时禁用按钮或隐藏按钮，并给出原因提示（而非静默无反应）。
  - [x] 统一复制按钮反馈状态：成功 `已复制`，失败 `复制失败`，并在超时后恢复。
  - [x] 明确按钮显示条件：仅在当前会话为 `client` 且目标节点 `role != client` 且已存在有效 `virtual_ipv4` 时显示。
  - [x] 通过 `serve` 启动（本机 `server` 角色）时，不在任何节点显示该按钮。
- [x] Prompt 内容正确性
  - [x] 校验 `buildAIPrompt` 中 `连接信息` 与 `API Base URL` 使用的是目标端地址而非本机地址。
  - [x] 校验候选目标列表仅包含本次点击目标（或明确说明多目标策略）。
- [x] Prompt 文案收敛
  - [x] 按需求改为 3 行短模板（包含 endpoint、`/health` 检查与 raw `SKILL.md` 链接）。
- [x] 测试与回归
  - [x] 运行 `go test ./...` 回归通过。
  - [x] 回归验证“复制连接信息”“复制日志”“命令复制”流程未受本次改动影响。
  - [x] 客户端/服务端角色条件与按钮显示策略按规则生效。

## 验收标准

- [x] 点击“复制给 AI 的 Prompt”后，剪贴板内容中的 `连接信息` 指向目标 peer 可访问地址。
- [x] 在不满足复制条件时，按钮不显示或给出明确反馈，不再出现“点击无反应”。
- [x] 控制台无未处理 Promise 错误（unhandled rejection）。

## 发布状态

- [x] 0.3.1 目标达成，可进入发布流程。
